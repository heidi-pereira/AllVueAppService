apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: snowflake-agent-live-private-key
  namespace: live
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: azure-store
    kind: ClusterSecretStore
  target:
    name: snowflake-agent-live-private-key
  data:
  - secretKey: snowflake-agent-live-private-key
    remoteRef:
      key: AKS-AIRFLOW-SNOWFLAKE-AGENT-LIVE-PRIVATE-KEY
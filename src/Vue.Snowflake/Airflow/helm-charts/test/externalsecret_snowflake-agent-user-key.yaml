apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: snowflake-agent-private-key
  namespace: test
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: azure-store
    kind: ClusterSecretStore
  target:
    name: snowflake-agent-private-key
  data:
  - secretKey: snowflake-agent-private-key
    remoteRef:
      key: AKS-AIRFLOW-SNOWFLAKE-AGENT-PRIVATE-KEY
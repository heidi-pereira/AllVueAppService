apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-airflow-logs-azurefile-beta
  namespace: beta
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 5Gi
{
  "ConnectionStrings": {
    "VueReportingDatabaseConnection": "Server=.\\sql2019;Database=BrandVueReporting;Trusted_Connection=True;"
  },
  "AppConfiguration": {
    "ExcludedFilters": [ "range", "moving average", "average" ],
    "RemoveFilters": [ "start", "end", "scstart", "scend" ],
    "ProductInformation": [
      {
        "Name": "eatingout",
        "Description": "Eating Out"
      }
    ],
    "AppendProductNameToRoot": true,
    "ReportingApiAccessToken": "This local token must be replaced in Octopus to be the same for Vue and Reporting"
  },
  "Egnyte": {
    "BearerToken": "",
    "Subdomain": "migglobal",
    "StorageRootUrl": "https://migglobal.egnyte.com/app/index.do#storage/files/1",
    "ReportsMainFolder": "/Shared/Systems/Dashboards/ReportsLocal"
  },
  "SavantaLogging": {
    "CommonSettings": {
      "AppName": "BrandVueReporting",
      "Environment": "Dev"
    },
    "LogAggregatorSinkSettings": {
      "LogEventLevel": "Error",
      // The below is a hack so this section is removed by logging
      // as if it's not here we can't override it in octopus :(
      "ApiKey": "<not in development>"
    },
    "ConsoleSinkSettings": {
      "LogEventLevel": "Information"
    },
    "FileSinkSettings": {
      "LogEventLevel": "Information"
    }
  }
}